// import KeepAwake from "react-native-keep-awake";

export const changeKeepAwake = keepAwake => {
  // if (keepAwake) {
  //   KeepAwake.activate();
  // } else {
  //   KeepAwake.deactivate();
  // }
};
