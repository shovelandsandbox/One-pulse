import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    viewTimer: {
        flexDirection: "row",
        marginTop: 60
    },
    timer: {
        color: "#515B61",
        fontSize: 24,
        fontWeight: "bold"
    },
    timerText: {
        color: "#818181",
        fontWeight: "600",
        fontSize: 14,
        marginLeft: 8,
        marginTop: 8
    },
});

